<h1>Question saved!</h1>
<h2><%= @q.title %></h2>
<h2><%= @q.body %></h2>
<%= link_to "Edit", edit_question_path(@q) %>

<!-- # Rails is doing this with Javascript, on the client side -->
<%= link_to("Delete", question_path(@q), {method: :delete, data: {confirm: "Are you sure?"}}) %>
